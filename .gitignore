node_modules/
dist/
build-artifacts/
*.log
.DS_Store
.vscode/
.idea/
*.tsbuildinfo
.env
.env.local

# Local pretrained-weight cache (bulb downloads checkpoints here on first run;
# never committed — see the transfer-learning spec's hosting note).
/weights/

# Excluded from publication. Specs are working design notes; .claude/.github
# are local config.
.typebulb/
/typebulbs-tentative
.github/
.claude/
.claude-paste
.claude-bulb
specs/
temp/
*.bin
