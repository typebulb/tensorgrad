node_modules/
dist/
build-artifacts/
*.log
.DS_Store
.vscode/
.idea/
*.tsbuildinfo
.env
.env.local

# Local pretrained-weight cache (bulb downloads checkpoints here on first run;
# never committed — see the transfer-learning spec's hosting note).
/weights/

# Excluded from publication. .claude/.github are local config.
# (specs/ and .paste are ignored via .git/info/exclude so they stay out of
# git but remain visible to Claude Code's @ picker — see that file.)
.typebulb/
.github/
.claude/
CLAUDE.md
temp/
*.bin
# ...except bulb weight assets under the wholesale-whitelisted antypica folder
# (.gitignore outranks .git/info/exclude, so the exception has to live here).
!typebulbs/u/antypica/**/*.bin
# agent switcher (local Claude Code model override)
.claude/settings.local.json
.claude/.cc-switcher.json